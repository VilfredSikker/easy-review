# easy-review-mcp-darwin-arm64

Prebuilt `er-mcp` binary for darwin-arm64. Do not install directly — it is an optional dependency of [`easy-review-mcp`](https://www.npmjs.com/package/easy-review-mcp).
